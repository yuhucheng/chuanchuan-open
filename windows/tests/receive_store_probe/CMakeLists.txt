cmake_minimum_required(VERSION 3.14)
project(receive_store_probe LANGUAGES CXX)
enable_testing()
add_executable(receive_store_probe receive_store_probe.cpp)
target_compile_features(receive_store_probe PRIVATE cxx_std_17)
target_compile_options(receive_store_probe PRIVATE /W4 /WX /utf-8 /EHsc)
target_compile_definitions(receive_store_probe PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
target_link_libraries(receive_store_probe PRIVATE bcrypt advapi32 ntdll mincore)
add_test(NAME receive_store_probe COMMAND receive_store_probe)
